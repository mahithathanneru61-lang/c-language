#include <stdio.h>
#define PI 3.14159 
float calculateArea(int radius); 
int main() {
    int r = 5;
    float area;
    area = calculateArea(r);
    printf("Radius: %d, Area: %.2f\n", r, area);
    return 0; 
}
float calculateArea(int radius) {
    return PI * radius * radius;
}
